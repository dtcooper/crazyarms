<icecast>
    <location>{{ settings.ICECAST_LOCATION|default:'The World' }}</location>
    <admin>{% if settings.ICECAST_ADMIN_EMAIL %}{{ settings.ICECAST_ADMIN_EMAIL }}{% else %}admin@{{ DOMAIN_NAME }}{% endif %}</admin>
    <hostname>localhost</hostname>

    <limits>
        <clients>{{ ICECAST_MAX_CLIENTS|default:'2500' }}</clients>
        <sources>{{ ICECAST_MAX_SOURCES|default:'25' }}</sources>
        <queue-size>524288</queue-size>
        <client-timeout>30</client-timeout>
        <header-timeout>15</header-timeout>
        <source-timeout>10</source-timeout>
        <burst-size>65535</burst-size>
        {% if ICECAST_MAX_BANDWITH %}
            <max-bandwidth>{{ ICECAST_MAX_BANDWITH }}</max-bandwidth>
        {% endif %}
    </limits>

    <authentication>
        <source-password>{{ settings.ICECAST_SOURCE_PASSWORD|default:settings.SECRET_KEY }}</source-password>
        <relay-password>{{ settings.ICECAST_RELAY_PASSWORD|default:settings.SECRET_KEY }}</relay-password>

        <admin-user>admin</admin-user>
        <admin-password>{{ settings.ICECAST_ADMIN_PASSWORD|default:'hackme' }}</admin-password>
    </authentication>

    <hostname>localhost</hostname>

    <listen-socket>
        <port>8000</port>
    </listen-socket>
    <fileserve>1</fileserve>
    <paths>
        <basedir>/usr/share/icecast</basedir>
        <logdir>/var/log/icecast</logdir>
        <webroot>/usr/share/icecast/web</webroot>
        <adminroot>/usr/share/icecast/admin</adminroot>
        <alias source="/" dest="/index.html"/>
    </paths>

    <logging>
        <accesslog>-</accesslog>
        <errorlog>-</errorlog>
        <loglevel>3</loglevel>
    </logging>

    <security>
        <chroot>0</chroot>
    </security>
</icecast>
