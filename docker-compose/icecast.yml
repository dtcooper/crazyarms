services:
  admin:
    depends_on:
      - icecast

  icecast:
    container_name: icecast
    build:
      context: ./icecast
      dockerfile: Dockerfile
    ports:
      - '8000:8000'
    volumes:
      - services_config:/config:ro
