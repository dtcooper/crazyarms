services:
  nginx:
    depends_on:
      - harbor-telnet
  harbor-telnet:
    container_name: harbor-telnet
    build:
      context: ./liquidsoap
      dockerfile: Dockerfile.harbor-telnet
    restart: always
    environment:
      TZ: ${TIMEZONE:-US/Pacific}
